# Data Sync Check v1.9.0

## Step 2 workflow and reusable table selections

- Moved Comparison Mode into Step 2 so scope is chosen while selecting tables.
- Kept the selected comparison mode visible as a read-only summary in Step 3.
- Moved Common into More options with SQL Server-only and PostgreSQL-only filters.
- Added named table selections with automatic loading from the selector.
- Restores every saved table across pagination pages in one action.
- Restores saved manual comparison keys for the selected tables.
- Prevents a saved selection from being applied to different database/schema details.
- Supports updating and deleting saved table selections.
- Keeps connection profiles and table selections independent.
- Updated application, health, documentation, setup, and package identity to v1.9.0.
