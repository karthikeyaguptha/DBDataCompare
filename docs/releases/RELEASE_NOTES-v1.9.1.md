# Data Sync Check v1.9.1

## Step 2 and Step 3 refinements

- Keeps Common, Only in SQL Server, and Only in PostgreSQL directly visible.
- Removes the Step 2 table-availability More options menu.
- Clears all selected tables when No saved table selection is chosen.
- Clears saved manual comparison keys with the table selection.
- Gives Batch Size the same Step 3 card design as every other run summary.
- Preserves the existing saved-selection, pagination, comparison, and report behavior.
- Updates application, health, documentation, setup, and package identity to v1.9.1.
