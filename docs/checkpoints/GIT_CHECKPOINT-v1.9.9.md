# Data Sync Check v1.9.9 Git checkpoint

```bash
git add .
git commit -m "refactor: organize project directory structure"
git push origin main

git tag -a v1.9.9 -m "Organize project directory structure"
git push origin v1.9.9
```
