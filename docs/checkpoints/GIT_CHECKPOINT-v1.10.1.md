# Git checkpoint — v1.10.1

- Release type: patch
- Base release: v1.10.0
- Scope: Settings request fix and regression coverage
- Validation: 83 automated tests passed

## Checkpoint

```bash
git commit -m "fix: open settings without connection"
git tag -a v1.10.1 -m "Fix Settings request handling"
```
