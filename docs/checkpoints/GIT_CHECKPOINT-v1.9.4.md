# Data Sync Check v1.9.4 Git checkpoint

Use one checkpoint for selectable one-sided reconciliation rows:

```bash
git add .
git commit -m "fix: allow one-sided reusable table selections"
git push origin main

git tag -a v1.9.4 -m "Allow one-sided reusable table selections"
git push origin v1.9.4
```
