# Data Sync Check v1.9.3 Git checkpoint

Use one checkpoint for selectable reusable table reconciliation:

```bash
git add .
git commit -m "feat: refine reusable table selection reconciliation"
git push origin main

git tag -a v1.9.3 -m "Refine reusable table selection reconciliation"
git push origin v1.9.3
```
