# Data Sync Check v1.9.2 Git checkpoint

Use one checkpoint for portable table templates and JSON transfer:

```bash
git add .
git commit -m "feat: add portable table templates and reconciliation"
git push origin main

git tag -a v1.9.2 -m "Add portable table templates and reconciliation"
git push origin v1.9.2
```
