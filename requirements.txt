# Data Sync Check v1.9.7
# Supported runtime: CPython 3.13 x64 on Windows.
# setup.bat installs binary packages only to prevent local C/C++ compilation.
Flask==3.1.3
waitress==3.0.2
python-dotenv==1.2.2
pyodbc==5.3.0
psycopg[binary]==3.3.4
tzdata==2026.1
