# Data Sync Check v1.9.5 Git checkpoint

Use one checkpoint for the reconciliation filtering and reopen workflow:

```bash
git add .
git commit -m "fix: refine reusable table reconciliation workflow"
git push origin main

git tag -a v1.9.5 -m "Refine reusable table reconciliation workflow"
git push origin v1.9.5
```
