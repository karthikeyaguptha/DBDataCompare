# Data Sync Check v1.9.8 Git checkpoint

```bash
git add .
git commit -m "fix: paginate report overview at ten rows"
git push origin main

git tag -a v1.9.8 -m "Paginate report overview at ten rows"
git push origin v1.9.8
```
